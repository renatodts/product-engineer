# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
next-env.d.ts
out/
.expo/
.turbo/
*.tsbuildinfo

# Test / coverage
coverage/
playwright-report/
test-results/

# Env
.env
.env.local
.env.*.local
.env.test

# Editor / OS
.DS_Store
*.log
npm-debug.log*
pnpm-debug.log*

# Local agent / skill tooling
# .claude/ is tracked so the shared harness (commands, vendored skills) travels with the repo.
# Keep only personal/secret Claude files local.
.claude/settings.local.json
.claude/*.local.json
.agents/.skill-lock.json.backup
