// TODO: Define cross-app API contracts (Zod schemas) as projects require them.
// Keep this package framework-neutral (no UI, no server-framework imports) so it
// stays CommonJS-importable by NestJS apps (ADR-002). See ADR-020.
export const PACKAGE_NAME = '@product-engineer/shared-contracts';
