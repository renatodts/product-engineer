# TODO: Replace with a real multi-stage build wired to the monorepo once the API has features.
FROM node:22-alpine AS base
WORKDIR /app

# Placeholder: real build copies pruned workspace output.
COPY package.json ./
CMD ["node", "-e", "console.log('placeholder image — implement build')"]
